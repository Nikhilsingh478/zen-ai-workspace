**JARVIS — Step 3: Cleanup + Documentation**

Read the entire project structure first using `ls -la` and `find . -maxdepth 2 -type f` before touching anything. Do not delete a single file until you have listed and reviewed everything.

---

## PART 1 — FILE CLEANUP

### SQL Files
Find all `.sql` files anywhere in the project. Create a new folder `supabase/migrations/`. Move every `.sql` file into it. Do not delete any — just consolidate. If there is a `supabase/` folder already, just create `migrations/` inside it.

### Safe To Delete — Root Clutter Only
Only delete files matching these exact patterns — nothing else:

- Any `.txt` files in root that are clearly paste dumps (names starting with `Pasted-`)
- Any `*.log` files in root
- Any `.tmp` files anywhere
- `dist/` folder if present in root (build output, regenerated by `npm run build`)
- Any duplicate config files like `vite.config.ts.bak` or `*.bak`

**NEVER delete:**
- `src/` — entire source folder, untouchable
- `public/` — untouchable
- `node_modules/` — untouchable
- `package.json`, `package-lock.json`, `tsconfig.json`, `vite.config.ts`
- `index.html`
- `.env` or any secrets file
- `supabase/` folder contents except to reorganize SQL files

If unsure about any file — keep it. When in doubt, do not delete.

---

## PART 2 — WRITE DOCUMENTATION

Create one single file: `docs/JARVIS_COMPLETE.md`

This must be a complete technical reference covering the entire current state of the project. Write it properly — not a summary, a full reference document a developer could use to understand and work on this codebase from scratch.

Cover these sections in order:

**1. Project Overview** — what AI Metrics is, single-user personal AI OS, tech stack table (React 19, Vite 7, TanStack Router, Tailwind v4, Framer Motion 11, Supabase JS v2, Gemini API, Kokoro TTS, OpenWakeWord)

**2. All 12 Pages** — for each page: route path, purpose, key features, Supabase tables used, AI integration if any

**3. JARVIS Complete Architecture** — this is the most important section, cover fully:
- Audio state machine (all 6 states, transition rules)
- Conversational runtime (4 modes, cooldown system, dialogue policy)
- Gemini function calling (all 7 tools, executeToolCall flow, thought_signature handling)
- Session management (startSession, endSession, lazy creation, empty session cleanup)
- Memory system (jarvis_memory table, 5 types, recall flow)
- Kokoro TTS (Web Worker architecture, am_adam voice, fallback chain)
- OpenWakeWord (hey_jarvis model, threshold 0.35, load-then-start sequence)
- Extended Window (animation sequence, 4-step cinematic flow, reverse animation)
- History viewer (session card click, loadSessionMessages, read-only mode)

**4. Database Schema** — all tables with column names, types, constraints, indexes

**5. Environment Variables** — all VITE_ variables, what each does, which are required

**6. Known Limitations** — browser TTS fallback quality, Kokoro first-load 82MB download, OpenWakeWord Chrome/Edge only, mobile tap-to-talk only

Keep it factual. No fluff. A developer should be able to read this and understand the entire system.

---

## NON-NEGOTIABLE

- Do not delete anything you are not 100% certain is safe to delete
- The SQL migration folder must be `supabase/migrations/`
- The docs file must be `docs/JARVIS_COMPLETE.md`
- Zero changes to any source code — this is documentation and cleanup only
- After completing, run `ls supabase/migrations/` and `ls docs/` and confirm both exist