JARVIS — Fix: Kokoro Embeddings + Wake Word Debounce + WebSocket + Voice Loading Read src/workers/kokoro-worker.js, src/lib/jarvis.ts, and src/lib/horizon.ts fully before touching anything. FILES TO TOUCH src/workers/kokoro-worker.js src/lib/jarvis.ts src/lib/horizon.ts FIX 1 — KOKORO SPEAKER EMBEDDINGS 404 Problem: Failed to fetch speaker embeddings: 404 — the .bin file URL is broken. In src/workers/kokoro-worker.js, replace loadSpeakerEmbeddings() with a version that tries multiple CDN sources in order: javascriptasync function loadSpeakerEmbeddings() { // Multiple CDN sources for the same embeddings file // bdl = CMU Arctic male speaker — natural male voice const EMBEDDING_URLS = [ // Primary — Hugging Face datasets CDN 'https://huggingface.co/datasets/Matthijs/cmu-arctic-xvectors/resolve/main/cmu_us_bdl_arctic-wav-arctic_a0009.bin', // Fallback 1 — different file from same dataset 'https://huggingface.co/datasets/Matthijs/cmu-arctic-xvectors/resolve/main/cmu_us_bdl_arctic-wav-arctic_a0002.bin', // Fallback 2 — slt female but better than nothing 'https://huggingface.co/datasets/Matthijs/cmu-arctic-xvectors/resolve/main/cmu_us_slt_arctic-wav-arctic_a0001.bin', ] for (const url of EMBEDDING_URLS) { try { const response = await fetch(url) if (!response.ok) { console.warn(`[Kokoro Worker] Embeddings 404 at: ${url}`) continue } const buffer = await response.arrayBuffer() console.log(`[Kokoro Worker] Embeddings loaded from: ${url}`) return new Float32Array(buffer) } catch (err) { console.warn(`[Kokoro Worker] Embeddings fetch failed: ${err.message}`) } } throw new Error('All speaker embedding sources failed') } Also update the load handler to continue attempting embeddings even if pipeline loads first — use Promise.allSettled instead of Promise.all so one failure doesn't abort both: javascriptconst [pipeResult, embResult] = await Promise.allSettled([ pipeline('text-to-speech', 'Xenova/speecht5_tts', { progress_callback: (p) => { if (p.status === 'downloading') { self.postMessage({ type: 'loading', message: `Loading voice... ${Math.round(p.progress ?? 0)}%` }) } } }), loadSpeakerEmbeddings() ]) if (pipeResult.status === 'rejected') { throw new Error(`Pipeline failed: ${pipeResult.reason.message}`) } if (embResult.status === 'rejected') { throw new Error(`Embeddings failed: ${embResult.reason.message}`) } synthesizer = pipeResult.value speakerEmbeddings = embResult.value FIX 2 — WAKE WORD DETECTING MULTIPLE TIMES PER UTTERANCE Problem: Three detections for one "Hey Jarvis" utterance — 0.84, 0.47, 0.80 scores within milliseconds. The engine fires multiple events as audio frames slide through the model window. Fix: Add a module-level debounce guard in src/lib/jarvis.ts inside the detect event handler: typescriptlet _lastWakeWordAt = 0 const WAKE_DEBOUNCE_MS = 3000 // ignore detections within 3s of last one // Inside startWakeWordDetection(), in the detect handler: engine.on('detect', ({ keyword, score }: { keyword: string, score: number }) => { const now = Date.now() // Debounce — ignore rapid re-detections if (now - _lastWakeWordAt < WAKE_DEBOUNCE_MS) { console.log(`[WakeWord] Debounced detection: ${keyword} (score: ${score.toFixed(2)})`) return } _lastWakeWordAt = now console.log(`[WakeWord] Detected: ${keyword} (score: ${score.toFixed(2)})`) if (_state.voiceState === 'idle' && _state.enabled) { transitionAudioState('listening') startCommandListening() } }) Also reset _lastWakeWordAt when conversation ends so a fresh wake word works immediately after cooldown: typescript// In endConversation(): _lastWakeWordAt = 0 FIX 3 — WEBSOCKET TEARDOWN (PERMANENT) Problem: The isMounted guard in horizon.ts keeps getting lost across rebuilds. Fix: In src/lib/horizon.ts, find setupRealtime and completely replace it with a version that is robust against rapid mount/unmount: typescript// Module-level — persists across component lifecycles let _realtimeMounted = false let _realtimeTimer: ReturnType<typeof setTimeout> | null = null let _realtimeChannel: ReturnType<typeof supabase.channel> | null = null function setupRealtime(): void { // Mark as mounted immediately _realtimeMounted = true // Cancel any pending setup from a previous rapid unmount if (_realtimeTimer) { clearTimeout(_realtimeTimer) _realtimeTimer = null } // 150ms delay — if component unmounts before this fires, teardown cancels it _realtimeTimer = setTimeout(() => { if (!_realtimeMounted) return // Don't create duplicate channels if (_realtimeChannel) return _realtimeChannel = supabase .channel('horizon_tasks_realtime') .on( 'postgres_changes', { event: '*', schema: 'public', table: 'horizon_tasks' }, (payload) => { if (!_realtimeMounted) return // existing handler logic — do not change this handleRealtimeEvent(payload) } ) .subscribe() }, 150) } function teardownRealtime(): void { _realtimeMounted = false if (_realtimeTimer) { clearTimeout(_realtimeTimer) _realtimeTimer = null } if (_realtimeChannel) { supabase.removeChannel(_realtimeChannel) _realtimeChannel = null } } Do not change handleRealtimeEvent or any other horizon logic — only the setup/teardown pattern. FIX 4 — BROWSER TTS USES DEFAULT VOICE ON FIRST CALL Problem: First TTS call uses "default" voice because getVoices() returns empty array until the browser fires voiceschanged event. The current wait logic isn't reliable. Fix: In src/lib/jarvis.ts, replace the voice loading wait in the browser TTS fallback with a proper voice cache: typescript// Module-level voice cache let _cachedMaleVoice: SpeechSynthesisVoice | null = null let _voicesLoaded = false // Call this once on module init — pre-loads voices function preloadVoices(): void { const tryLoad = () => { const voices = window.speechSynthesis.getVoices() if (voices.length > 0) { _voicesLoaded = true _cachedMaleVoice = selectBestMaleVoice(voices) console.log('[TTS] Voices pre-loaded:', _cachedMaleVoice?.name ?? 'default') } } // Try immediately tryLoad() // Also listen for the event in case voices aren't ready yet if (!_voicesLoaded) { window.speechSynthesis.onvoiceschanged = () => { tryLoad() window.speechSynthesis.onvoiceschanged = null // fire once } } } // Rename existing getBestMaleVoice to selectBestMaleVoice // (same logic, just called internally now) function selectBestMaleVoice( voices: SpeechSynthesisVoice[] ): SpeechSynthesisVoice | null { // existing getBestMaleVoice logic unchanged } // Update the browser TTS fallback in playNext() to use cache: const voice = _cachedMaleVoice ?? selectBestMaleVoice( window.speechSynthesis.getVoices() ) if (voice) utterance.voice = voice Call preloadVoices() at the bottom of the module — after all function declarations — so it runs when JARVIS first loads, not on first speech: typescript// At module level, after all declarations: if (typeof window !== 'undefined') { preloadVoices() } FIX 5 — CONVERSATION COOLDOWN STILL ENDING TOO FAST Problem: Looking at the log sequence — auto-resume fires, but cooldown expires before user can respond. The issue is the cooldown timer starts when TTSQueue.playNext() drains the last sentence from the queue — but the last sentence may still be playing for several seconds after the queue drains. Fix: In src/lib/jarvis.ts, inside TTSQueue.playNext(), move startConversationCooldown() to fire from the onend event of the last utterance, not when the queue check happens: typescriptprivate playNext(): void { if (this.queue.length === 0) { this.isPlaying = false this.stopKeepAlive() // Queue is empty but last utterance may still be speaking // Don't call startConversationCooldown() here return } const sentence = this.queue.shift()! const isLastSentence = this.queue.length === 0 // ... existing utterance setup ... utterance.onend = () => { clearTimeout(timeoutId) if (isLastSentence) { // This was the final sentence — speech is truly done now transitionAudioState('idle') startConversationCooldown() // cooldown starts AFTER last word spoken } else { this.playNext() } } // ... rest of existing implementation ... } For Kokoro path (Web Audio API), add the same logic at source.onended: typescriptsource.onended = () => { if (isLastSentence) { transitionAudioState('idle') startConversationCooldown() } else { this.playNext() } } The isLastSentence check captures whether the queue was empty when this sentence started playing — meaning this is the final sentence. Cooldown only starts after that last word finishes. NON-NEGOTIABLE REQUIREMENTS WAKE_DEBOUNCE_MS = 3000 — exact value _lastWakeWordAt resets to 0 in endConversation() Promise.allSettled in kokoro worker — not Promise.all WebSocket teardown guard uses module-level variables — not inside any React component preloadVoices() called at module init — not on first speech isLastSentence captured before queue.shift() modifies queue length — capture it as const isLastSentence = this.queue.length === 1 BEFORE the shift startConversationCooldown() has exactly one call site per speech path — the existing call in playNext() queue-drain check must be removed, replaced by the onend/onended approach Zero TypeScript errors Do not touch any other file You said: main thing is that voice is not working wake word works properly you sure ?